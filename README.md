# LCS-Gene-Similarity
This C++17 project implements the classic Longest Common Subsequence algorithm in two forms: a full reconstruction tool that returns the actual LCS string, and a space-optimised version that builds a pairwise similarity matrix (High/Medium/Low/Dissimilar) for a set of gene sequences.
